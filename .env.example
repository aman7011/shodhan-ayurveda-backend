# Environment Variables Template
# Copy this file to .env (which is gitignored) and fill in your actual values

# Database Configuration
DATABASE_URL=jdbc:postgresql://localhost:5432/shodhanayurveda
DATABASE_USERNAME=your_db_username
DATABASE_PASSWORD=your_db_password

# Mail Configuration
MAIL_USERNAME=your-email@gmail.com
MAIL_PASSWORD=your-app-password

# Spring Profile
SPRING_PROFILES_ACTIVE=dev

# Optional: Override default mail settings
# MAIL_HOST=smtp.gmail.com
# MAIL_PORT=587

# CORS Configuration (comma-separated list of allowed origins)
# For development with React on port 3000:
CORS_ALLOWED_ORIGINS=http://localhost:3000
# For production, set this to your actual frontend URLs:
# CORS_ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com
